# Build output
target/
build/
node_modules/
__pycache__/
*.pyc
.venv/
dist

# IDE
.idea/
*.iml
.vscode/

# Local configuration and secrets
.env
*.local

# OS
.DS_Store
Thumbs.db

# Organizers dataset and any raw data never go into the repo (size, license); load facts through scripts/
# Anchored (not bare `data/`) so it doesn't also catch frontend/src/data/, which is tracked.
/data/
/frontend/data/
dataset*.zip
*.zip

# Tool caches accidentally created at the repo root
/.vite/
*.tsbuildinfo
