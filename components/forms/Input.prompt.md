Text field for registration, team names, search and numeric filters.

```jsx
<Input label="Название команды" placeholder="Manticore" hint="От 3 до 5 человек" />
<Input label="Почта" error="Укажите корпоративную почту" prefix={<Icon name="mail" size={16} />} />
```
