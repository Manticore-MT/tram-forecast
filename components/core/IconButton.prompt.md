Circular icon-only control for toolbars, card corners and map overlays.

```jsx
<IconButton label="Скачать CSV" icon={<Icon name="download" />} />
```

32 / 40 / 48px. `ghost` inside dense toolbars, `secondary` on surfaces, `primary` only for a floating primary action.
