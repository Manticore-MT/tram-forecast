The one place passenger load is expressed as colour. Reuse it everywhere load appears so map, table and card agree.

```jsx
<LoadMeter value={0.82} />
```

Labels: Свободно / Комфортно / Умеренно / Плотно / Перегружено.
