# Открытые вопросы

Вопросы, которые пока не решены и требуют уточнения (у Никиты по данным, на созвоне по продукту
и т.д.), в отличие от [`product-decisions.md`](product-decisions.md), где зафиксировано решённое.
Не смешивать: сюда попадает только то, что реально требует чьего-то ответа, а не общие рассуждения.

## ВСМ: данные отсутствуют?

`product-decisions.md` § «ВСМ (high-speed rail) impact» держит фичу под условием: делать её только
если в датасете организаторов реально есть данные по ВСМ. Беглая проверка (grep по README и
`spravochniki` в архиве датасета на «всм»/«высокоскорост») ничего не нашла — но это проверка кода,
не ответ команды или организаторов.

**Нужно узнать/решить:** подтвердить у Никиты (или у оргов) — данных по ВСМ в датасете правда нет,
или они просто не размечены явным словом и лежат под другим названием? От ответа зависит, снимать
ли фичу с MVP-скоупа или нет.

## Виджеты на карте (drag & drop, интерактивный список)

Идея — перетаскиваемые виджеты поверх карты вместо фиксированных панелей. Отложено как nice-to-have:
на баллы не влияет. Подготовлено: все панели экрана диспетчера размещаются через один компонент
`frontend/src/shared/Floating.tsx` (слоты top/left/right/bottom), так что сделать их перетаскиваемыми
или авто-раскладываемыми — правка в одном месте.

**Нужно решить:** делаем ли вообще, и если да — какие виджеты в списке.

## График для диапазона пассажиропотока

Не решено, стоит ли добавлять отдельный график, визуализирующий диапазон (от–до) пассажиропотока —
пока просто мысль, без решения "да/нет".

**Нужно решить:** нужен ли отдельный график вообще, или диапазон и так читается из уже показанных
базового уровня/прогноза/отклонения. Связано с пунктом "пик → диапазон" ниже — возможно, одно и то же
решение закроет оба вопроса.

## Величина рекомендации ("+1 трамвай" vs "добавить N трамваев")

Сейчас в `product-decisions.md` зафиксирован бинарный сигнал (+1 / без изменений / −1) по порогам
загрузки 80%/40%. Возник вопрос, можно ли считать не бинарный сигнал, а конкретное число трамваев.

**Нужно узнать:** есть ли в датасете организаторов данные о текущем интервале движения / доступном
парке по маршруту? Без этого посчитать "сколько добавить" нельзя — уточнить у Никиты, входит ли это
в данные вообще, до того как проектировать API под это.

## Пик → диапазон

В зонах внимания предложено показывать не точку пика, а диапазон (от–до), в котором нужно обратить
внимание. Пик как точное время остаётся на слайдере в детальном виде остановки.

**Нужно решить:** это чисто визуальное решение (тот же ряд точек, просто по-другому его читаем и
рисуем на фронте) — или это значит, что бэк должен отдавать в `/api/attention` не одну точку пика,
а начало/конец окна как отдельное поле? От ответа зависит, нужно ли менять контракт (`ml-contract.md`,
`openapi.json`) или это чисто фронтовая задача.

## Участок между остановками (сегмент маршрута)

Бриф прямо требует детализацию «по маршруту, остановке/участку» (целевые задачи, требования к API,
критерий 4) — это напомнила Анастасия. `product-decisions.md` (§ "Map interaction") требует, чтобы
перегоны между остановками были кликабельны, как и остановки. Но датасет даёт прогноз только
маршрут × час — источника данных на уровне участка (и даже остановки) нет.

Предложение фронта (Адис, 26.09), **требует подтверждения бэка и ML**: участок — сосед остановки на
одном уровне иерархии (сеть → маршрут → {остановка | участок}), участок = перегон между двумя
соседними остановками. Отдельная ручка, например
`GET /api/routes/{routeId}/segments/{fromStopId}/{toStopId}/forecast` (если принимать произвольную
пару остановок — интервал «от A до B» получается бесплатно).

**Нужно узнать:**

- У ML/Никиты: что такое «загрузка участка» — наполненность вагона на перегоне (вошло минус вышло)?
  Валидации фиксируют только вход — можно ли оценить выходы? Если нет — участок временно считается
  производной от двух соседних остановок (тогда это та же величина, что у остановки).
- У Кирилла: делаем ли ручку для участка, и в какой форме.

## Колонка с пересадками (датасет организаторов)

На созвоне с оргами (2026-09-25) спросили про колонку с пересадками: `NaN` — значит пересадки не
было? А значение "НГПТ" — значит пересадка была? Ответ организаторов зафиксирован в конспекте, но
непонятен ("тут ничего не понял по комментариям если честно" — со слов присутствовавшего).

**Нужно узнать:** переспросить у оргов или найти описание колонки в документации к датасету — от
этого зависит, как считать пересадки (входит ли это вообще в MVP-скоуп).

## stopId бэка vs. код остановки у фронта

`ml-contract.md` подразумевает, что `stopId` в ответах API — id из "датасета остановок, которым
фронт рисует карту". В коде (`StubMlForecastClient`) сейчас `stopId` — чистая заглушка ("R1-S2",
порядковый номер в фейковой сетке маршрутов), реального маппинга на датасет остановок нет.

У фронта раньше (`tram-network.json`) не было вообще никакого id остановки — только название и
координаты. Починили: `scripts/build-tram-network.mjs` терял `stopCode` ("Код остановки" из
датасета организаторов) при сборке, хотя сам код там был. Теперь `stopCode` есть у каждой
остановки в `tram-network.json`.

**Нужно узнать у Кирилла/ML:** когда переходим на реальные (не stub) данные — `stopId` будет
строиться по этому же `stopCode` организаторов, или по другому id? Если по другому — нужен либо
общий id на обеих сторонах, либо матчинг по координатам/названиям (с ручной чисткой, если названия
не совпадут 1-в-1). Без ответа: прогноз/зоны внимания могут отображаться у неправильной остановки
на карте.

## Требования к презентации/питчу

Орги сказали, что "в идеальной презентации" есть критерии на стек, архитектуру, проверочные и
достигнутые результаты — но сами критерии не были зачитаны/зафиксированы дословно.

**Нужно узнать:** попросить у оргов сам список критериев или шаблон презентации (в конспекте
упомянуто "по формату презентации написать в чат, мб есть шаблон") — чтобы Олег не готовил питч
вслепую.

## Единица `forecast` / `baseline`

`ml-contract.md` держит это открытым. Фронт сейчас **предполагает** «пассажиры за период точки»:
`пасс/ч` на дне, `пасс/сут` на 7 днях и месяце, `пасс/мес` на годе (`SCALE_UNITS` в
`frontend/src/features/dispatcher/time.ts`); итог за интервал — сумма точек в «пасс».

**Нужно подтвердить у Кирилла/Никиты:** это пассажиры за период, а не среднее в час и не доля
загрузки. Если иначе — поменять подписи в одном месте.

## «Было → стало» в «Сценарии» vs отменённый US5

US5 (сравнение сценариев «база vs what-if») отменён на созвоне 19.09 (`product-decisions.md`
§ Scope cuts). Панель «Сценарий» показывает прогноз без поправок → с поправками в выбранной точке и
контур прогноза без поправок на полосе времени — это второй запрос с коэффициентами по умолчанию.
Фронт считает это не US5 (отдельный экран двух параллельных сценариев), а прямое выполнение
критерия 2в брифа («менять коэффициенты и сразу видеть, как меняется прогноз») — но это интерпретация
фронта.

**Нужно подтвердить на созвоне:** не противоречит ли это решению по US5.

## Формат пользовательской выгрузки (`/api/export`)

Кнопка «Выгрузить» — в шапке приложения рядом с «Выйти» (решение фронта, 26.09): открывает сводку
«что уйдёт в файл» (объект, период, интервал, сценарий) и скачивает CSV из `/api/export` с теми же
параметрами, что на экране. Эндпоинт сейчас отдаёт только CSV.

**Нужно решить с Кириллом/Никитой:**

- Нужен ли вариант «в формате сабмита» (тот CSV, что уходит оргам в раздел Data Science)? Для демо:
  «вот файл, который мы отправили, выгружен прямо из сервиса».
- Числа в выгрузке сейчас не округлены (`deviation_pct = 3.875968992248062`, дробный `forecast`),
  а орги требуют целый прогноз. Для пользовательской выгрузки тоже стоит округлять.
- Нужен ли XLSX (бриф: «CSV и/или XLSX» — CSV достаточно).

## Данные для экрана «Обзор» (стейкхолдер)

Раскладка «Обзора» собрана по must-have из `product-analysis-full.md` (дашборд стейкхолдера). Часть
пунктов упирается в то, чего нет в API.

**Вопросы Кириллу:**

- Можно ли вернуть факт (`actual`) в `/api/routes`? Сейчас там только `forecast`/`baseline`
  (`SlimPoint`), поэтому «факт ↔ прогноз» по всей сети не построить (только по маршруту), а сравнение
  «к прошлому периоду» идёт прогноз к прогнозу, а не к факту.
- Можно ли отдавать MAPE / RMSE / R² в `/api/model/stats`? Аналитик хочет «понятный агрегированный
  показатель, MAPE/RMSE/R² — в детализации»; сейчас есть только `wape` и `wapeScore`.

**Вопросы Кириллу/Никите:**

- Внешние факторы на уровне сети: какие факторы учла модель за период? Сейчас `factors` есть только
  в прогнозе остановки.
- Топ **остановок** по отклонению упирается в сопоставление `stopId` с картой (см. «stopId бэка vs.
  код остановки у фронта») — пока «Обзор» показывает только топ маршрутов.

Вне MVP, без вопросов: индекс бесшовности (US11 — метрика не определена), ВСМ (данных нет).

## Блоки старого дашборда, которые ждут данных

Старый дашборд удалён из кода; всё полезное перенесено или закрыто. Остались блоки, для которых нет
реальных данных. Код лежит в git-тегах, достать: `git show <тег>:<путь>`.

- **Доверительный интервал прогноза** (полоса вокруг линии прогноза) — тег `legacy-dashboard`,
  `frontend/src/products/forecast-dashboard/Charts.tsx` → `ForecastChart` (`band`). В старом коде
  интервал был выдуманный. **Вопрос Никите:** может ли модель отдавать интервал?
- **Внешние факторы карточками** (календарь, погода, события) — тег `legacy-dashboard`,
  `…/Screens.tsx` → `EXTERNAL_FACTORS`. Тексты были выдуманные. Ждёт ответа, какие факторы модель
  учла на уровне сети (см. «Данные для экрана «Обзор»»).
- **Важность признаков модели** и **история обучений** — тег `legacy-dashboard-mock`,
  `…/Screens.tsx` → `ModelView`. **Вопрос Никите:** может ли ML отдавать важность признаков и
  историю версий модели?

